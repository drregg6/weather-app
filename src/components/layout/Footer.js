import React from 'react';

export default function Footer() {
    return (
        <footer style={footerStyle}>
            <h6>Footer</h6>
        </footer>
    );
}

const footerStyle = {
    color: '#fff',
    background: '#333',
    padding: '50px'
}